<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Quotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.Quotes" parent="Base.Theme.Quotes" />

    <style name="CustomTabText" parent="Widget.Design.TabLayout">
        <item name="android:textSize">20sp</item> <!-- Set the desired text size -->
        <item name="android:radius">20dp</item>
        <item name="android:textStyle">bold</item> <!-- Set any additional text style attributes -->
    </style>
</resources>